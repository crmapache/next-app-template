import { {{pascalCase name}}Wrap } from './{{pascalCase name}}.elements';
import { {{pascalCase name}}Props } from './{{pascalCase name}}.types';
import { useStyles } from './{{pascalCase name}}.styles';

export const {{pascalCase name}} = ({}: {{pascalCase name}}Props) => {
  const styles = useStyles();

  return <{{pascalCase name}}Wrap>{{pascalCase name}}</{{pascalCase name}}Wrap>;
};
